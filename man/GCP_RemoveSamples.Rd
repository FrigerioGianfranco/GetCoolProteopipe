% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GCP_RemoveSamples.R
\name{GCP_RemoveSamples}
\alias{GCP_RemoveSamples}
\title{Remove samples from the dataset}
\usage{
GCP_RemoveSamples(
  GCPlist,
  remove_samples = NULL,
  keep_samples = NULL,
  column_samples = 1,
  remove_groups = NULL,
  keep_groups = NULL,
  name_column_groups = NULL
)
}
\arguments{
\item{GCPlist}{a list created with the ImportOutputMaxQuant function.}

\item{remove_samples}{NULL or a character containing the name of samples to remove.}

\item{keep_samples}{NULL or a character containing the name of samples to keep.}

\item{column_samples}{an integer number or a charcter of length 1. The number or name of the column to look at the elements passed by remove_samples or keep_samples.}

\item{remove_groups}{NULL or a character containing the name of groups to remove.}

\item{keep_groups}{NULL or a character containing the name of groups to keep.}

\item{name_column_groups}{NULL or character of length 1. The name of the column of the sampleINFO table containing the sample groups. It must be specified if you used remove_groups or keep_groups.}
}
\value{
the GCPlist with samples removed from the sampleINFO, and intensities data frames.
}
\description{
Remove samples from both the intensities and  SampleINFO data frames. Only one of the arguments among remove_samples, keep_samples, remove_groups, or keep_groups has to be specified.
}
\examples{
\dontrun{

# specify what to remove:

GCPlist00rm1 <- GCP_RemoveSamples(GCPlist = GCPlist00,
                                  remove_samples = c("S2", "S5"),
                                  keep_samples = NULL)

# or specify what to keep (same output as above):

GCPlist00rm2 <- GCP_RemoveSamples(GCPlist = GCPlist00,
                                  remove_samples = NULL,
                                  keep_samples = c("S1", "S3", "S4", "V1", "V2", "V3", "V4", "V5"))

}


}
