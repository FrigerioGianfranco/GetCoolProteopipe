# Checking if the GCPlist is suitable

Throw error if GCPlist has something wrong.

## Usage

``` r
checkGCPlist(GCPlist)
```

## Arguments

- GCPlist:

  a list created with the ImportOutputMaxQuant function.

## Value

if everything is ok, it does nothing. It only throws an error in case of
something wrong.
